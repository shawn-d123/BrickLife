# BrickLife's web app reads no environment variables at build or run time.
# predictions.json, the sprites, fonts and room art are all static files
# baked into the bundle — there is no API key, backend URL, or secret to
# configure. This file exists so that fact is explicit rather than silent.
